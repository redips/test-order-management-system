# docker-compose.override.yml.example
# 
# Copy this file to docker-compose.override.yml for local development customizations
# This file is ignored by git and won't be committed
#
# Example customizations:

version: '3.8'

services:
  # Uncomment to enable Xdebug for PHP debugging
  # php:
  #   environment:
  #     XDEBUG_MODE: debug
  #     XDEBUG_CONFIG: client_host=host.docker.internal

  # Uncomment to use different ports
  # nginx:
  #   ports:
  #     - "8081:80"

  # database:
  #   ports:
  #     - "3308:3306"

  # Uncomment to persist database data between restarts
  # database:
  #   volumes:
  #     - ./docker/mysql-data:/var/lib/mysql

  # Uncomment to add phpMyAdmin
  # phpmyadmin:
  #   image: phpmyadmin/phpmyadmin
  #   container_name: order_management_phpmyadmin
  #   environment:
  #     PMA_HOST: database
  #     PMA_USER: symfony
  #     PMA_PASSWORD: symfony
  #   ports:
  #     - "8082:80"
  #   networks:
  #     - order_network
  #   depends_on:
  #     - database
